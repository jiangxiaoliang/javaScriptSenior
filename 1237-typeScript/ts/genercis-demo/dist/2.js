let vueOptions = {
    el: '#app',
    template: '<div></div>'
};
let reactOption = {
    props: 'props',
    state: 'state'
};
function getOption(obj, key) {
    return obj[key];
}
getOption(vueOptions, 'edfafa');
getOption(reactOption, 'fadfaf');
